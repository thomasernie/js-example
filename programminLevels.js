//Begineers - 3 - 6 months

const basicsStrong = true
const errorFixing = function copyErrorCheckINGoogleRead() {
    const googleResult = []
    const resultIntIme = googleResult.find(result === 'solution')
    if (resultIntIme > 4) // more than 4 hrs
    {
        return "getHelp"
    }
}

// Intermeditate - 6 months to 2 Years
const specializeOneTech = true
const deepKnowledgeinOneTech = true
const knowsMostOfCommonModules = true
const independentMostOfTheTime = true

// Architect - 2 Years
const startSettingFramework = true
const startSettingStandards = true
const startReviewinGCode = trues